first need to install tkinter for gui by using bash or cmd

1. use "pip install tkinter" or "py -m pip install tkinter"

2. after install tkinter next is "pip install -r requirements.txt" or "py -m pip install -r requirements.txt" to install cryptography lib

3. run by py filecrypt.py

update instruction on 11/21/2024 by nattapon 